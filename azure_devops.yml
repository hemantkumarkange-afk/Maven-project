
trigger:
- main
- master
- devops

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: mvn validate
  displayName: 'Validate Maven Project'

- script: mvn compile
  displayName: 'Compile Maven Project'
